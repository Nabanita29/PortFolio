function toggleMenu() {
  const menu = document.querySelector(".menu-links");
  const icon = document.querySelector(".hamburger-icon");
  menu.classList.toggle("open");
  menu.classList.toggle("open");
}

// an inbuilt function that is called when the page is loaded
// meaning of the javascript code below is that when the page is loaded
// the toggleMenu function is called
